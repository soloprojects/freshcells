import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#89CFF0',
      contrastText: '#0047AB',
    },
    secondary: {
      main: '#000000',
      contrastText: '#FFFFFF',
    },
  },
});
